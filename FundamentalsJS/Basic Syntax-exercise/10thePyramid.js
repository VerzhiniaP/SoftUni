function pyramid(arg1 ,arg2){
    let base = Number(arg1)


    let stone = 0
    //the bulk is made of stone
    let marble = 0
    // outer layer
    let lapisLazulli = 0
    // every 5th step
    let gold = 0
    // the final step
    let width = 0
    //1x1 blocks 
    let length = 0
    let incresement = Number(arg2)
    // == height
    // w  * l = base every 2nd step is reduced - 2 block (2 * 1 = 2)

    base = width * length
    let block = 1
    let countBlocks = 0

    




}
pyramid("11", "1")